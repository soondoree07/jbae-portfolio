# 진행 상황 (2026-08-25 KST 기준)

배정혜(J.Bae) 화가 포트폴리오. 바닐라 HTML/CSS/JS + `data.json`.
**개인 도메인 `jbae.art` 로 서비스 중.** UI 개편은 끝났고 지금은 다듬는 단계다.

- 공개 주소: **https://jbae.art** (www·http·옛 github.io 주소 모두 여기로 넘어온다)
- 로컬 확인: `python3 -m http.server 8899` → http://localhost:8899

## 8월 25일에 한 것 (커밋 1건, push·배포 완료)

- **시기 설명 5곳을 채웠다** — 작가님께 받은 2025 · 2022 · 2021 · 2018–2020 · 2017
- **12개 시기 문체를 `-다` 체로 통일** — 받은 글이 `-다` 체(작가 노트 문체)라
  기존 네 곳(2024 · 2023 · 2011 · 2009)의 **어미만** 바꿨다. 내용은 건드리지 않았다.
  사용자가 세 안(기존을 `-다`로 / 새 글을 `-습니다`로 / 섞어 두기) 중 첫째를 골랐다
- 원문 오타 2건 교정 — 2025 겹친 공백, 2017 마침표 뒤 띄어쓰기 누락
- 데스크톱 1400px · 모바일 390px 실제 렌더로 확인. 가장 긴 2017(204자)도 넘치지 않고,
  빈 세 곳은 자리를 그대로 지킨다

## 8월 24일에 한 것 (커밋 7건, 전부 push·배포 완료)

- **도메인 `jbae.art` 연결** — 가비아에서 구입, DNS(A 4개 + www CNAME) 설정,
  `CNAME` 파일 추가, 인증서 발급 후 HTTPS 강제. canonical·OG 주소도 새 도메인으로 교체
- **약력에 제8회 개인전 추가** (2026.09 구띠커피갤러리). 제15회 After Hours 는 이미 있어 건너뜀
- **'비어있음' 글자 제거** — 자리(다섯 줄 높이)는 그대로. 12개 시기 위치가 1px 도 안 변한 것 확인
- **약력 제목 옆 항목 수 제거** (학력 2, 개인전 8 … 같은 숫자)
- **QR 새 도메인으로 재제작 + 나비 로고 삽입** (아래 별도 항목)
- **모바일 문의 구획** — 제목과 연락처 붙이고 이메일 위·인스타 아래로 세로 배치
- **구획 제목과 내용 사이 거리 32px 로 통일** — 시기 칸을 가운데 맞춤에서 위 맞춤으로
  바꿔 연도가 작품 이미지 윗변과 나란해졌다

## 8월 22일에 한 것 (커밋 15건)

- **무드 8종을 실제 작품으로 렌더해 비교** → 3번 "자연 결" 확정
  (비교 화면: https://claude.ai/code/artifact/d522a091-9f47-4907-ba0e-62819d5fa796)
- Claude Design 내보내기가 비표준(`<x-dc>`·`{{ }}`·support.js 의존)이라 못 쓴다고 판단, **직접 구현으로 전환**
- **사이트 전면 재구축** — 구조·CSS·JS 새로 짜고 `data.json` 연결. `style.css`/`script.js` → `css/` 4개 + `js/` 6개
- **작품 비율 맞춤 격자** 도입 (세로 Masonry 폐기), 썸네일 122장 생성 69MB → 11MB
- SEO·OG·파비콘·apple-touch-icon, 사이트 이동 **QR 5종**(`docs/qr/`)
- **형태를 각지게** — 곡률·그림자·기울임 전부 제거, 버튼을 밑줄 글자로
- **화면 문구 정리** — 히어로 캡션·스크롤 안내·구획 부제·"작품 N점" 전부 제거
- 연도별 문구를 설명으로 통일, 없는 8곳은 '비어있음' + 빈 줄 넷
- 작품 제목 한 줄 고정, **문의 구획을 눌러서 복사 방식으로 교체** (동작 확인 완료)
- **모바일 검증** 320~768px — 가로 넘침 0, 터치 44px 미만 0
- **전화번호 삭제**, 이메일 유지 결정
- 헤드리스 렌더 검증으로 버그 6건 발견·수정 (아래 참고)

## 막힌 지점

없다. **남은 세 시기(2016 · 2014–2015 · 2012–2013) 설명을 기다리는 중.**

## QR 코드 (2026-08-24 확정)

`docs/qr/` · 윈도우 바탕화면 `jbae-QR` 폴더에도 같은 파일이 있다.

| 파일 | 쓸 곳 |
|---|---|
| `jbae-qr.svg` / `.png` | 기본. 모래빛 + 나비 |
| `jbae-qr-mono.svg` / `.png` | 흑백 |
| `jbae-qr-plain.svg` / `.png` | 나비 없는 안전판 (1.5cm 이하로 작게 쓸 때) |
| `jbae-qr-plain-mono.svg` / `.png` | 나비 없는 흑백 |
| `nabi-logo.png` | 나비만 1:1 1024px |

- 버전 3, **29×29 모듈**, 오류 정정 **H**. 주소가 16자로 짧아 이전(41×41)보다 성기다
- 가운데 나비 판은 **정확히 11×11 모듈**. 가려지는 면적 14.4% (H 허용 30%)
- **판 크기를 비율(%)로 잡지 말 것.** 10.68모듈처럼 소수가 되면 격자 중간이 잘려
  가장자리에 반쪽 칸과 실선 자국이 남는다. 반드시 홀수 모듈 수로 잡는다
- 나비는 카카오톡 이미지에서 **이어진 덩어리 중 가장 큰 것만 골라내** 글자를 뺀 것이다
- **사용자가 실제 폰으로 찍어 연결 확인 완료**
- 최소 인쇄 크기 2cm, 둘레 여백 자르지 말 것

- 공개 주소: https://jbae.art
- 로컬 확인: `python3 -m http.server 8899` → http://localhost:8899
  (`data.json`을 fetch로 읽고 JS 모듈을 써서 파일 더블클릭으로는 안 열린다)

## 확정된 디자인

**무드 "자연 결"** — 무드 8종을 실제 작품으로 렌더해 비교한 뒤 선택.
비교 화면: https://claude.ai/code/artifact/d522a091-9f47-4907-ba0e-62819d5fa796

| 항목 | 값 |
|---|---|
| 바탕 / 면 | `#E8E0D3` / `#F2ECE1` |
| 글자 / 보조 | `#3A332A` / `#857B6B` |
| 선 / 진한 선 | `#D2C6B3` / `#B9A98F` |
| 강조 | `#9C6B4A` |
| 글씨 | 고운바탕(제목) + 본고딕(본문, 300 웨이트) |

**형태는 각지게.** 사용자 지시로 2차 수정에서 둥근 모서리·그림자·기울임을 전부 걷어냈다.
경계는 1px 선으로만 잡는다. 버튼 형태를 쓰지 않고 밑줄 달린 글자로 처리한다.
`css/tokens.css`에 곡률·그림자 변수가 아예 없다 — 되살리지 말 것.

## 구조

```
index.html   히어로 → 작품 연보(12시기) → 작가 → 약력 → 문의
year.html    한 시기의 작품 전체. ?year=2023 처럼 붙여 연다
css/  tokens · base · layout · components
js/   data · motion · home · year · cv · lightbox
tools/build_image_meta.py   이미지 크기 측정 + 썸네일 생성
docs/qr/                    사이트 이동 QR 5종
```

- 작품 연보에 "이 시기 보기" 버튼은 **없다.** 연도 숫자 자체가 링크다
- 연도 상세 = 비율 맞춤 격자 + "순서대로 보기"(라이트박스 슬라이드)

## 화면에서 일부러 뺀 것 (되살리지 말 것)

사용자가 하나씩 지목해 없앤 것들이다.

- 히어로의 작품 제목·재료·크기 캡션, "내려서 작품 보기" 스크롤 안내
- 구획 부제 셋 — 작품 연보 "시기를 고르면…", 약력 "제목을 누르면…", 문의 "작품 구매, 전시 협의…"
- **"작품 N점" 표시 전부** — 메인 연보·연도 상세·바닥글 셋 다
- 복사 줄의 "눌러서 복사" 안내 글자
- 둥근 모서리·그림자·기울임 (`css/tokens.css`에 변수 자체가 없다)
- 전화번호 (기획서 방침. `data.json`에는 남아 있으나 화면에 쓰지 않는다)

## 간격 규칙 (2026-08-24)

- **구획 제목과 내용 사이는 32px** (`--s6`). 네 구획 모두 같다
- 시기 칸은 **위 맞춤**(`align-items:start`)이다. 가운데 맞춤으로 되돌리면
  연도가 이미지 높이만큼 내려가 제목에서 164px 떨어져 보인다
- 모바일 문의 구획은 세로 배치, 두 줄 간격 32px.
  복사 알림이 줄 아래에 뜨므로 이보다 좁히면 겹친다 (여유 6px 뿐)

## 개인정보 처리 (2026-08-22 결정)

- **전화번호** — `data.json`에서 **삭제했다.** 화면·데이터 어디에도 없다. 다시 넣지 말 것
  (단, 과거 커밋에는 남아 있다. 히스토리를 다시 쓰지는 않기로 함)
- **복사 기능** — 사용자가 실제로 눌러 **"복사했어요"가 뜨는 것을 확인**했다 (2026-08-22)
- **이메일** — **남기기로 결정.** 작품 구매·전시 문의를 받으려면 연락 수단이 필요하고,
  정적 사이트라 화면에 보이는 값은 레포에도 있을 수밖에 없다.
  인스타그램만으로는 DM을 안 쓰는 사람이 연락할 길이 없어진다

## 모바일 반응형 (2026-08-22 검증)

320 / 360 / 390 / 414 / 768px 다섯 폭에서 실제 렌더로 측정. 결과는 전부 통과.

- 가로 넘침 0건, 갤러리 1열 전환 정상, 본문 16px
- 파노라마는 높이 190px 유지 + 가로 스크롤
- 라이트박스 이미지가 설명을 덮지 않고 화면 밖으로 넘치지 않음
- **터치 크기 44px 미만 0건** — 작던 링크들은 여백 대신 가상 요소로
  눌리는 범위만 넓혔다. 여백을 주면 밑줄이 글자에서 떨어져 디자인이 흐트러진다

## 연락처 다루는 방식

- **작가 구획** = 링크. 메일 주소를 누르면 메일 앱, 인스타 아이디를 누르면 인스타로 간다
- **문의 구획(맨 아래)** = 눌러서 복사. mailto 는 메일 프로그램이 없는 사람에게
  아무 일도 일어나지 않아서 폐기했다. 복사가 막히면 글자를 자동 선택해 준다 (`js/copy.js`)
- 인스타 아이디는 `data.json`의 주소에서 뽑아 쓴다 (`instagramHandle`). 손으로 적지 말 것

## 연도별 문구 규칙

- 연도 아래에는 **그 시기 설명만** 온다. 작품 제목으로 대신 채우지 않는다
- **문체는 `-다` 체로 통일돼 있다** (2026-08-25). 새 설명을 넣을 때도 `-다`로 맞춘다
- 설명 있음 9곳: **2025 · 2024 · 2023 · 2022 · 2021 · 2018–2020 · 2017 · 2011 · 2009**
  (101~204자. 가장 긴 2017도 데스크톱 8줄로 넘치지 않는다)
- 비어 있음 3곳: 2016 · 2014–2015 · 2012–2013
  → **빈 줄 다섯만큼(140px) 자리를 남긴다** ('비어있음' 글자는 8/24에 없앴다).
    `.chapter-desc.is-empty{min-height:calc(5 * 1.75em)}` 이 실제 값이다
- 연도 상세 페이지에서는 설명이 없으면 자리째 없앤다

## 시기 대표 이미지

`data.json`의 각 시기 `thumbnail`에 **손으로 지정된 값**이다. 자동으로 뽑히지 않고 매번 바뀌지도 않는다.
12곳 모두 그 해 작품 중 하나이고, 목록에서의 위치는 1·2·4·5·6·9번째로 제각각이다.
바꾸려면 해당 시기의 `thumbnail` 경로만 다른 작품으로 교체하면 된다.

## ★ 작품 이미지 비율 — 레이아웃의 전제

세로형이 거의 없다. **가로형 56점 / 정사각 62점 / 세로형 4점.**
「구름에 기대 꿈을 그리다」는 **8.28 : 1 파노라마**(100×45cm 15점 세트).

→ 핀터레스트식 세로 Masonry를 쓰면 안 된다. 비율에 따라 칸 수를 바꾼다
   (`js/data.js`의 `spanOf`: 3:1 이상은 한 줄 통째, 1.55 이상은 2칸, 나머지 1칸).
   좁은 화면에서 파노라마는 높이를 지키고 가로로 민다.

**작품을 추가하면 반드시 `python3 tools/build_image_meta.py` 를 돌린다.**
이미지 크기를 재서 `data.json`에 `w` `h` `thumb`를 넣고 썸네일을 만든다.
빼먹으면 격자가 틀어지고 로딩 중 화면이 밀린다.

## 다음 액션 (이어할 작업)

0. **작가 소개 카드를 없앨지 결정** — 지금 제목→소개 "글자"까지가 65px 로,
   다른 구획(32px)보다 멀다. 카드 테두리·배경을 없애면 32px 로 맞고 담백해진다.
   사용자에게 제안해 둔 상태이며 답을 못 받았다
1. **남은 세 시기 설명 받아 넣기** — 2016 · 2014–2015 · 2012–2013 (목록은 아래 참고).
   해당 시기의 `description` 값에 넣으면 그 자리가 글로 채워진다. **`-다` 체로 맞출 것**
2. **작품 순서 바꾸기** — `data.json`의 각 시기 `works` 배열 순서가 곧 화면 순서다
3. **작품 크기(칸 배분) 조정** — `js/data.js`의 `spanOf` 기준값을 만지거나
   개별 작품에 칸 수를 직접 지정하는 필드를 추가하는 방법이 있다
4. **시기 대표 이미지 교체** — 바꾸고 싶은 시기의 `thumbnail` 경로만 다른 작품으로

## 설명이 필요한 시기 — 남은 3곳 (작품 25점)

| 시기 | 점수 | 작품 |
|---|---|---|
| 2016 | 6 | 민화1 ~ 민화6 (제목이 연번뿐이라 설명이 특히 필요하다) |
| 2014–2015 | 7 | Blooming · Fresh · 기억-봄날 · 산-그림자 · 향기로운산 · 여운1 · 여운2 |
| 2012–2013 | 12 | I Have a Dream · Colorful Flowers · 나비의꿈2 · 따로 또 같이 · I Have a Dream2 · 따로 또 같이2 · 마음 깊은 곳 · 몽환 · 유혹 · 행복한 망상 · 꿈꾸는 나비 · Rainbow,Black, Sunshine |

채워진 9곳은 모두 **"그 시기에 무엇을 들여다봤는지"를 1인칭 `-다` 체로** 쓴 글이다.
같은 결로 받으면 잘 붙는다. 참고 — 2017: "바람, 물, 하늘, 새, 꽃과 같은 이미지들은
결국 나의 기억과 감정을 담아내는 매개가 된다…"

**8/25에 채운 5곳** (작가님 원문, 오타 2건만 교정):
2025 169자 · 2022 164자 · 2021 156자 · 2018–2020 142자 · 2017 204자

## 오늘 잡은 버그 (같은 실수 반복하지 말 것)

- `.page`와 `.hero`가 같은 `padding` 속기형을 두고 충돌 → 모바일 상단 여백이 통째로 사라짐.
  **위아래는 `padding-block`, 좌우는 `padding-inline`으로 나눠 쓴다**
- 파노라마에 준 `max-width:none`이 격자 칸을 1572px으로 부풀려 모든 작품이 확대됨.
  **`grid-template-columns`에 `minmax(0,1fr)`, 항목에 `min-width:0`**
- 라이트박스 이미지가 설명을 덮음 — `max-height:100%`가 grid 자동 행 높이를 기준으로 잡혀 무력화.
  **stage를 flex로** 두어야 100%의 기준이 실제 남은 높이가 된다
- 한글이 낱말 중간에서 끊김 → `word-break:keep-all`
- `#about` 같은 앵커로 들어오면 빈 화면 → 렌더가 끝난 뒤 한 번 더 이동시킨다 (`jumpToHash`)
- 히어로 캡션이 다른 작품 이름을 가리킴 — 시기 `thumbnail`과 `works[0]`은 다르다

## 남은 숙제

| 항목 | 내용 |
|---|---|
| **시기 설명 3곳 비어 있음** | 2016 · 2014–2015 · 2012–2013. 자리만 비어 보인다. 작가님 회신 대기 중 |
| 더미 시기 4개 | 2026·2010·2008·2007은 picsum 자리표시자라 화면에 안 나온다. 처리 방침 미정 |
| 원본 이미지 무게 | `images/` 69MB. 격자는 썸네일(11MB)을 쓰지만 라이트박스는 원본을 쓴다. 최대 6.8MB |

## 참고

- `data.json` = `{ artist:{name,nameEn,tagline,bio,email,phone,instagram,cv:{...}}, years:[{id,year,description,thumbnail,thumb,w,h,works:[...]}] }`
- 약력 각 줄은 **두 칸 이상 띄어쓰기**로 연도/이름/장소를 나눈다. 이 구분에 맞춰 표로 그려진다
- 약력 분량: 학력 2 / 개인전 7 / 단체전 70 / 수상 3 / 강의 2 / 소장처 13. 단체전은 기본으로 접혀 있다
- `artist_portfolio`는 2026-06-27 삭제. 원본 .tif 132개는 `~/artist_originals`에 보존
